# TODO: доведение системы до соответствия спецификациям из `docs/diploma-spec`

Ниже перечислены требования из файлов `ТЗ_Мирошниченко_Денис.docx`, `Отчет_ВКР_Мирошниченко.docx`, `ТП_Мирошниченко_Денис.docx`, `ПМИ_Мирошниченко_Денис.docx`, `РО_Мирошниченко_Денис.docx` и `Презентация.pptx`, которые сейчас отсутствуют или реализованы только частично.

Проверка делалась по текущему коду backend, frontend, миграциям БД, `docker-compose` и Helm chart'ам.

## Высокий приоритет

- [ ] Добавить полноценное редактирование и на краулинг.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`, `Отчет_ВКР_Мирошниченко.docx`, `Презентация.pptx`.
  Сейчас: публичный API содержит только `ListJobs`, `CreateJob`, `GetJob`, `GetJobExportFileURL`; во frontend есть создание и просмотр, но нет update/delete flow.

- [ ] Добавить табличный просмотр именно извлеченных результатов парсинга, а не только списка задач.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx` для роли `READ` и общего требования просмотра собранных данных.
  Сейчас: UI показывает jobs, tasks и агрегаты по статусам задач; отдельного API/UI для просмотра нормализованных записей парсинга нет. Заготовки `records_handler.go` и `snapshots_handler.go` пустые.

- [ ] Добавить визуальный анализ собранных данных через web-интерфейс.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`.
  Сейчас: есть только operational analytics по задачам (`status/depth`), но нет визуализаций по самим собранным данным.

- [ ] Реализовать сохранение обработанных данных в БД как структурированных записей.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`; также это описано в `Отчет_ВКР_Мирошниченко.docx` и `ТП_Мирошниченко_Денис.docx`.
  Сейчас: parser сохраняет результат JSON в object storage и пишет в БД только `result_object_key`; таблиц уровня `extracted_records` в миграциях нет.

- [ ] Добавить управление воркерами из web-интерфейса: остановка/drain, принудительное завершение и сценарий перезапуска.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`, `Отчет_ВКР_Мирошниченко.docx`, `Презентация.pptx`.
  Сейчас: backend умеет `DrainWorker` и `ForceKillWorker`, но frontend показывает только мониторинг без управляющих кнопок; отдельного механизма restart нет.

- [ ] Добавить просмотр логов и метрик системы из административного интерфейса или хотя бы штатную интеграцию/переходы к соответствующим экранам.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`.
  Сейчас: observability-стек есть через Prometheus/Grafana/OpenSearch, но в приложении нет route/page/api под логи и метрики.

- [ ] Добавить настройку параметров подключения к хранилищам данных из системы управления.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`.
  Сейчас: такие настройки задаются только через env/Helm values; `Queue Admin` покрывает очереди, но не PostgreSQL / MinIO / Redis / S3-подобные хранилища.

## Средний приоритет

- [ ] Добавить редактирование параметров парсинга для уже созданного задания.
  Источник требования: `Отчет_ВКР_Мирошниченко.docx`, `Презентация.pptx`.
  Сейчас: extraction spec и связанные настройки можно задать только при создании нового задания.

- [ ] Добавить выбор формата выходных данных при создании задания.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx` для роли `READ_WRITE`.
  Сейчас: export worker всегда формирует и JSON, и CSV; в конфиге задания нет поля выбора выходного формата.

- [ ] Добавить нормализацию дат на уровне extraction/transforms.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx` требует нормализацию дат, цен и числовых значений.
  Сейчас: поддержаны `parse_price`, `to_int`, `to_float`, но отдельной трансформации для дат нет.

- [ ] Изменить поведение при превышении rate limit на повторную постановку задачи в очередь.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`.
  Сейчас: fetch worker ждет `retry_after` внутри воркера и повторяет проверку локально, а не requeue'ит задачу обратно в брокер.

- [ ] Добавить фильтрацию и поиск по ключевым словам/времени именно для собранных результатов, а не только для jobs/tasks.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx` для роли `READ`.
  Сейчас: фильтры есть для jobs и tasks, но не для extracted data.

## Низкий приоритет / deployment gaps

- [ ] Довести deployment до полного покрытия заявленных вариантов брокеров сообщений.
  Источник требования: `ТЗ_Мирошниченко_Денис.docx`, `ТП_Мирошниченко_Денис.docx`, `Отчет_ВКР_Мирошниченко.docx`.
  Сейчас: `docker-compose.yaml` поднимает RabbitMQ и Kafka, но Helm infra chart не содержит Kafka; для `memory_broker` нет штатного deployment в compose/Helm.

- [ ] Если проект должен строго соответствовать описанию из отчета/ТП, добавить отдельные сущности и API для `page_snapshots` / `extracted_records`.
  Источник требования: `Отчет_ВКР_Мирошниченко.docx`, `ТП_Мирошниченко_Денис.docx`.
  Сейчас: вместо этого используются поля `minio_object_key` / `result_object_key` в `crawl_tasks`.

## Что уже реализовано и не требует TODO

- [x] Роли `READ`, `READ_WRITE`, `ADMINISTRATOR`.
- [x] Аутентификация и RBAC.
- [x] Создание заданий, расписание и cron.
- [x] JSON/CSV экспорт результатов.
- [x] Базовый мониторинг воркеров.
- [x] Queue admin для endpoint'ов и routing rules.
- [x] Rate limiting, retries, robots.txt, object storage, observability stack.

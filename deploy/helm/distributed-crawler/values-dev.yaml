# Development environment values
# Usage: helm install crawler ./deploy/helm/distributed-crawler -f ./deploy/helm/distributed-crawler/values-dev.yaml

# Use development log settings
config:
  log:
    level: debug
    env: development

  # Match .env settings
  messaging:
    broker: "rabbitmq"

  limiterType: "redis"

  minio:
    publicBaseURL: "http://localhost:9000"

  kafka:
    brokers: "localhost:9091"
    consumerGroup: "distributed-crawler"
    crawlTopicName: "crawl_queue"
    parsingTopicName: "parsing_queue"

  memoryBroker:
    addr: ":9095"
    capacity: 1000

  otel:
    enabled: false
    environment: "development"
    exporterEndpoint: ""
    exporterInsecure: true

  opensearch:
    enabled: false
    endpoint: ""
    index: "app-logs"
    flushIntervalSeconds: 5
    batchSize: 100

# Secrets matching .env values
secrets:
  postgres:
    password: "some-pwd-123"

  rabbitmq:
    password: "guest"

  minio:
    user: "minioadmin"
    password: "minioadmin"

  redis:
    password: "some_redis_pwd_123"

  auth:
    jwtSecret: "your-secret-key-change-this-in-production-make-it-long-and-random"
    defaultPassword: "12345678"

  queueSecrets:
    content: '{}'

# Lower resource requirements for development
ui:
  replicaCount: 1
  resources:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 20m
      memory: 32Mi

grpcServer:
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi

fetchWorker:
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi

parserWorker:
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi

exportWorker:
  replicaCount: 1
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 64Mi

# Subchart configuration (auth must match secrets above so DSN is consistent)
postgresql:
  auth:
    password: "some-pwd-123"
  primary:
    persistence:
      size: 1Gi
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 128Mi

rabbitmq:
  auth:
    password: "guest"
  persistence:
    size: 1Gi
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi

minio:
  auth:
    rootPassword: "minioadmin"
  persistence:
    size: 5Gi
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi

redis:
  auth:
    password: "some_redis_pwd_123"
  master:
    persistence:
      size: 1Gi
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 128Mi
